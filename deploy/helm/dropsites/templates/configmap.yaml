apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dropsites.fullname" . }}
  labels:
    {{- include "dropsites.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
