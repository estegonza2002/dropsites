apiVersion: v1
kind: Service
metadata:
  name: {{ include "dropsites.fullname" . }}
  labels:
    {{- include "dropsites.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dropsites.selectorLabels" . | nindent 4 }}
